# Changelog

本文件记录 dsh-token-panel 的用户可见更新。格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [0.3.0] - 2026-08-14

### 新增

- **按模型分别计价**：会话行、按日/按月统计按各会话实际使用的模型（v4-flash / v4-pro 等）套用对应价格表，切换过模型的会话不再整段按单一模型计价
- **auto 价格模式**：2026-08-17 零时（北京时间）前自动使用 flat 旧价，之后自动切换 DeepSeek 官方峰谷价（高峰 9-12 / 14-18），底部徽章同步显示「标准价 / 高峰价 / 空闲价」，无需手动改配置

### 变更

- 底部栏改为三列布局：状态 + 价格模式 ｜ 当前花费 ｜ 年月日 + 时间（重排 0.2.2 引入的「当前花费」）
- 统计视图排版精简：删除「汇总」「趋势」标题，累计消耗标签加大单行显示（0.2.2 排版重构的后续调整）
- 曲线 Y 轴重排：单位标签常驻、紧凑刻度（1B/500M）、当前值胶囊浮动在最新点旁，大数值不再重叠
- 统计与实时视图高度精确对齐；底部当前花费与统计口径统一

### 修复

- **预算进度条不可见**（0.2.2 引入的缺陷）：引用了不存在的主题 token（`state-success` 等），已替换为主题真实定义的带后缀变量，绿色进度/成本/徽章全部恢复显示
- **统计视图崩溃**（0.2.2 引入的缺陷）：React Hooks 顺序违规导致切换统计视图时面板消失
- **会话成本高估**：会话混用模型时整段按当前模型计价的问题（由按模型分别计价修复）
- 统计视图双层容器造成的多余空白与灰色滚动条条带（0.2.2 排版重构的遗留问题）

## [0.2.2] - 2026-08-14

### 新增

- **可编辑预算与余额**：统计视图点击数值行内编辑（回车保存 / Esc 取消），悬停气泡提示；余额按 token 消耗**本地估算递减**（累计基准法，刷新后保持），未设置时回退 API 余额
- **底部显示当前花费**：面板底部右侧统一显示累计估算花费
- **峰值读数**：实时趋势区显示窗口内峰值消耗速率（t/s）

### 变更

- **UI 排版重构**：分区小标题加大（13px）分隔「实时趋势 / 会话 / 汇总 / 趋势 / 按日」，大数字层级 + 分段控件，保留科技感（等宽字体/发光点缀）
- 会话列表默认只显示当前会话，展开才显示全部
- 按日 / 按月明细默认收起（0 行），点击展开全部

### 修复

- 移除统计视图旧的平铺布局与边框线（改留白分区）

## [0.2.1] - 2026-08-14

### 修复

- 会话行、账户余额悬停气泡全部改为自动消失样式（淡入约 1.5 秒后自动淡出，指针停留也不会一直显示），并移除浏览器原生 tooltip

## [0.2.0] - 2026-08-14

### 新增

- **会话花费显示**：实时视图每个会话行显示绿色 `¥` 估算花费，当前对话一眼可见
- **面板拖拽系统**：
  - 按住胶囊或面板头部栏拖动，位置跨刷新记忆
  - 允许把面板拖出屏幕边缘（上下左右），但始终保留一条可抓取的头部栏
  - 指针捕获 + 4px 阈值，快速拖拽不中断、单击不误触
- **长按位置菜单**（按住 0.6 秒）：
  - 回到默认位置（图标与文字随默认位置动态更新）
  - 「位置」二级菜单：**左上 / 右上 / 左下 / 右下**四角预设（按面板实际尺寸精确贴边）
  - **自定义位置**：选择后拖到任意位置松手即保存为默认，每次加载从那里出现
  - 菜单图标统一为 SVG 描边风格（与 DSH 设计语言一致）
- **Y 轴坐标**：所有曲线带纵坐标刻度与浅色网格线
- **自动缩放 Y 轴**：1/2/2.5/5×10ⁿ 取整 + 滞回（上升立即跟随、下降滞后），空闲时归零
- **当前值指示线**：曲线最新点带虚线引导线与实时数值胶囊标签
- **峰谷计价模式**：`priceMode: peak-offpeak` 按北京时间峰谷自动切换价格
- **TPS 显示**：胶囊与底部栏实时显示生成速度（t/s）
- **账户余额**：统计视图自动拉取 DeepSeek 余额（5 分钟缓存）
- **月预算**：`budgetMonthly` 配置后显示预算进度条，超支变红
- **历史会话持久化**：会话注册表落盘，重启后「展开全部」的历史会话不丢失

### 修复

- 曲线空闲时不再维持压力水平线，改为消耗增量（归零）
- 时间刻度不再被 SVG 边界裁切（自适应锚点 + 底部留白）
- 小数刻度保留一位（0.5 不再显示为 1）
- 统计视图切换即时刷新
- 拖拽与单击/长按手势冲突（单击打不开、拖后需点两次等）
- 预设角落偏上（改用实际尺寸计算）
- 拖出屏幕后无法抓回（把手保留可见）
- 单点打开面板（指针捕获时机修正）

### 变更

- 点「✕」收起面板不再重置位置（原位置保持；归位走长按菜单）
- 统计视图「按日/按月」显示全部日期与月份

## [0.1.0] - 2026-08-14

- 初版发布：实时视图（会话压力/累计/详情/曲线）、统计视图（按日/按月/持久化日志）、DeepSeek 官方价成本估算、中英双语界面
